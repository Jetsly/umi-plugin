{{{ importsAhead }}}
import Vue from 'vue';
import VueRouter from 'vue-router';
{{{ imports }}}

Vue.use(VueRouter)

let mode = `{{{ RouterRootComponent }}}`;

let routes = {{{ routes }}};

export default new VueRouter({
  mode,
  routes
})
